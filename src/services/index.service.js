const authenticateService = require('./authenticate.service')
const sendTicketEmailService = require('./sendTicketEmail.service')

module.exports = {
    authenticateService,
    sendTicketEmailService
}